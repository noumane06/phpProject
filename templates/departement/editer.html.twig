<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Hello </title>
    <link rel="shortcut icon" href="/Res/itachi-alt.ico" type="image/x-icon"/>
</head>
</html>
<h1>Editer des dep! ✅</h1>
<form action="{{ path("dep_HandleEdit",{id:dep.id}) }}" method="POST" >
    nom_departement<input type="text"  name="nom_dep"  value="{{ c.nom_dep }}" required><br>
    <input type="submit">
</form>
<a href="{{ path("Liste_dep") }}"> go back</a>